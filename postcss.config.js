module.exports = {
    plugins: [
        require('tailwindcss'),
        require('precss'),
        // require('cssnano')({
        //     preset: 'default',
        // }),
        require('autoprefixer'),
    ],
}
